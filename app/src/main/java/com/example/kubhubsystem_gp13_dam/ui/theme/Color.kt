package com.example.kubhubsystem_gp13_dam.ui.theme

import androidx.compose.ui.graphics.Color

// === COLORES PRINCIPALES ===
val Primary = Color(0xFF8E4D30)          // Color principal de la app
val Secondary = Color(0xFF77574A)        // Color secundario de la app
val Error = Color(0xFFBA1A1A)            // Color para errores

// === COLORES DE CONTAINERS ===
val PrimaryContainer = Color(0xFFFFDBCD)      // Fondo de elementos primarios
val SecondaryContainer = Color(0xFFFFDBCD)    // Fondo de elementos secundarios
val ErrorContainer = Color(0xFFFDDAD6)        // Fondo de elementos con error

// === SUPERFICIES Y VARIANTES ===
val Surface = Color(0xFFFFF8F6)           // Fondo general de la app
val SurfaceDim = Color(0xFFE8D6D0)        // Fondo ligeramente oscuro (para efectos)
val InverseSurface = Color(0xFF382E2A)    // Fondo inverso (ej: barras o tarjetas destacadas)

// === TEXTO SOBRE SUPERFICIES ===
val OnSurface = Color(0xFF231A16)         // Color del texto sobre surface
val OnSurfaceVariant = Color(0xFF53433E)  // Variante del texto sobre surface

// === BORDES / OUTLINES ===
val Outline = Color(0xFF85736D)           // Bordes generales
val OutlineVariant = Color(0xFFD8C2BA)    // Variante de bordes más clara

// === SOMBRAS ===
val Shadow = Color(0xFF000000)            // Color para sombras

// === COLORES RECOMENDADOS ADICIONALES ===
val OnPrimary = Color(0xFFFFFFFF)         // Color de texto sobre Primary
val OnSecondary = Color(0xFFFFFFFF)       // Color de texto sobre Secondary
val OnError = Color(0xFFFFFFFF)           // Color de texto sobre Error